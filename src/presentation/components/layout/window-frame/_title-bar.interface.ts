export enum WindowFocusState {
	Active = "active",
	Inactive = "inactive",
}

export enum WindowDisplayState {
	Normal = "normal",
	Maximized = "maximized",
	Minimized = "minimized",
	Fullscreen = "fullscreen",
}

export enum ButtonInteractionState {
	Normal = "normal",
	Hover = "hover",
	Pressed = "pressed",
	Disabled = "disabled",
}
